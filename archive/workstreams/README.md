# Archived Workstreams

This directory stores workstream docs that are still useful as historical architecture or transition context but are no longer authoritative for active development.

Current live backend truth belongs in the server repo:

- API behavior: `hidden-adventures-server/docs/contract.md`
- operational usage: `hidden-adventures-server/README.md`
- implemented schema: `hidden-adventures-server/db/migrations/`

Use the archived copies here only when you need earlier planning intent or design rationale.
